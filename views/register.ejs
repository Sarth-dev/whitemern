<!DOCTYPE html>
<html lang="Javascript">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Register page</title>
</head>

<body>
    <div class="w-full h-screen bg-zinc-800 text-white">
        <div class="logo w-full h-8 bg-[#FFB300] p-1 text-black">Logo</div>
        <div class="w-full h-full  bg-zinc-800 p-10">
            <form class="flex flex-col  gap-10" action="/register" method="post">
                <div class="flex justify-center gap-2">
                    <label for="username">Username:</label>
                    <input class="w-72 p-1 rounded-md outline-none bg-zinc-600" type="text" id="username" name="username" required>
                </div>
                <div class="flex justify-center gap-2">
                    <label for="email">Email:</label>
                    <input class="w-72 p-1 rounded-md outline-none bg-zinc-600"  type="email" id="email" name="email" required>
                </div>
                <div class="flex justify-center gap-2">
                    <label for="username">Password:</label>
                    <input class="w-72 p-1 rounded-md outline-none bg-zinc-600"  type="password" id="password" name="password" required>
                </div>
               <div class="flex justify-center">
                <input class="px-5 py-2 bg-blue-700 rounded-md w-fit" type="submit" value="Register">
               </div>
            </form>
        </div>
    </div>
    <!-- <script>
        function validateForm() {
            const username = document.getElementById('username').value.trim();
            const password = document.getElementById('password').value.trim();

            if (username === '') {
                alert('Please enter your username.');
                return false;
            }
    
           
            if (password === '') {
                alert('Please enter your password.');
                return false;
            } else if (password.length < 6) {
                alert('Password must be at least 6 characters long.');
                return false;
            }
    
           
            alert('Form submitted successfully.');
            return true;
        }
    </script> -->
</body>

</html>